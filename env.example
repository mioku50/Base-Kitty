# Production required: canonical app URL (no trailing slash)
# Preview: optional, host header fallback is used by auth verification
NEXT_PUBLIC_URL=https://base-kitty.vercel.app

# Production required for signed Mini App manifest accountAssociation
# Preview/dev may be left empty
FARCASTER_HEADER=
FARCASTER_PAYLOAD=
FARCASTER_SIGNATURE=

# Required for social profile enrichment and webhook app-key verification
NEYNAR_API_KEY=

# Optional: only needed for Neynar-hosted notification webhook workflows
NEYNAR_CLIENT_ID=

# Strongly recommended in production: persistent leaderboard storage.
# If empty, scores are kept only in memory and can reset between deploys/instances.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# Alternative Vercel KV aliases (same value pair)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Daily blessing claim config (required for claim flow)
# Base RPC endpoint, e.g. 
BASE_RPC_URL=https://mainnet.base.org

# Base mainnet addresses
DEGEN_TOKEN_ADDRESS=
CLAIM_CONTRACT_ADDRESS=

# Contract v2 expected (task-aware claim voucher: daily/streak/invite)
# DailyBlessingDistributor constructor: (token, signer)

# Daily reward in base units (10 DEGEN at 18 decimals)
DAILY_BLESSING_AMOUNT_RAW=10000000000000000000

# Task rewards in base units (optional; defaults are 1 and 2 DEGEN)
STREAK_BLESSING_AMOUNT_RAW=1000000000000000000
INVITE_BLESSING_AMOUNT_RAW=2000000000000000000

# Backend signer private key (0x-prefixed 32-byte hex)
CLAIM_SIGNER_PRIVATE_KEY=

# Optional: protect /api/cron/daily-reminder with ?secret=<value>
CRON_SECRET=
